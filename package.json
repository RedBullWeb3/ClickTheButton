{
  "name": "click-the-button",
  "version": "2.1.3.7",
  "description": "",
  "scripts": {
    "start": "rm -rf dist && parcel src/index.html",
    "deploy": "rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./  && gh-pages -d dist"
  },
  "author": "",
  "license": "JP2GMD",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.5.0"
  }
}
